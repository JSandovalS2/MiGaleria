name: galeria_personal
description: A new Flutter application.


publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_database: ^3.1.3
  firebase_storage: ^3.1.3
  image_picker: ^0.6.3+4
  intl: ^0.16.1
  flutter_launcher_icons: ^0.8.1
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
